import { ReverseWordPipePipe } from './reverse-word-pipe-pipe';

describe('ReverseWordPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseWordPipePipe();
    expect(pipe).toBeTruthy();
  });
});
